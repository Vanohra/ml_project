Pillow
torch
torchvision
numpy
pyyaml

# Required for SSIM in evaluate_paired.py
scikit-image

# Optional — install for additional evaluation metrics:
# pip install lpips           (LPIPS perceptual similarity — evaluate_paired.py)
# pip install piq             (NIQE + BRISQUE no-reference quality — evaluate_noref.py)

# Optional — required only for VIRAT video frame extraction:
# pip install opencv-python   (video reading — extract_virat_frames.py)
